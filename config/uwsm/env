# Arch graphical-session adapter. uwsm sources this file at session start.
# Keep shared environment in desktop-session so the Void adapter can use it.

shared_env="${XDG_CONFIG_HOME:-$HOME/.config}/desktop-session/env"
if [ -r "$shared_env" ]; then
  # shellcheck disable=SC1090
  . "$shared_env"
fi
