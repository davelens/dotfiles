# Shared session PATH; hardware overrides must be supplied by the machine.
export MISE_DATA_DIR="${MISE_DATA_DIR:-${XDG_DATA_HOME:-$HOME/.local/share}/mise}"
export PATH="${XDG_BIN_HOME:-$HOME/.local/bin}:$MISE_DATA_DIR/shims:$PATH"
