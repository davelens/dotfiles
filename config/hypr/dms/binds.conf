# === Application Launchers ===
bind = ALT, T, exec, wezterm
bind = ALT, space, exec, dms ipc call spotlight toggle
bind = ALT, V, exec, dms ipc call clipboard toggle
bind = ALT, M, exec, dms ipc call processlist focusOrToggle
bind = ALT, comma, exec, dms ipc call settings focusOrToggle
bind = ALT, N, exec, dms ipc call notifications toggle
bind = ALT SHIFT, N, exec, dms ipc call notepad toggle
bind = ALT, Y, exec, dms ipc call dankdash wallpaper
# bind = ALT, TAB, exec, dms ipc call hypr toggleOverview
bind = ALT, X, exec, dms ipc call powermenu toggle

# === Cheat sheet
bind = ALT SHIFT, Slash, exec, dms ipc call keybinds toggle hyprland

# === Security ===
# bind = SUPER ALT, L, exec, dms ipc call lock lock
bind = ALT SHIFT, E, exit
bind = CTRL ALT, Delete, exec, dms ipc call processlist focusOrToggle

# === Audio Controls ===
bindel = , XF86AudioRaiseVolume, exec, dms ipc call audio increment 3
bindel = , XF86AudioLowerVolume, exec, dms ipc call audio decrement 3
bindl = , XF86AudioMute, exec, dms ipc call audio mute
bindl = , XF86AudioMicMute, exec, dms ipc call audio micmute
bindl = , XF86AudioPause, exec, dms ipc call mpris playPause
bindl = , XF86AudioPlay, exec, dms ipc call mpris playPause
bindl = , XF86AudioPrev, exec, dms ipc call mpris previous
bindl = , XF86AudioNext, exec, dms ipc call mpris next

# === Brightness Controls ===
bindel = , XF86MonBrightnessUp, exec, dms ipc call brightness increment 5 ""
bindel = , XF86MonBrightnessDown, exec, dms ipc call brightness decrement 5 ""

# === Window Management ===
bind = ALT, Q, killactive
bind = ALT, F, fullscreen, 1
bind = ALT SHIFT, F, fullscreen, 0
bind = ALT SHIFT, T, togglefloating
bind = ALT, W, togglegroup

# === Focus Navigation ===
bind = ALT, left, movefocus, l
bind = ALT, down, movefocus, d
bind = ALT, up, movefocus, u
bind = ALT, right, movefocus, r
bind = ALT, H, movefocus, l
bind = ALT, J, movefocus, d
bind = ALT, K, movefocus, u
bind = ALT, L, movefocus, r

# === Window Movement ===
bind = ALT SHIFT, left, movewindow, l
bind = ALT SHIFT, down, movewindow, d
bind = ALT SHIFT, up, movewindow, u
bind = ALT SHIFT, right, movewindow, r
bind = ALT SHIFT, H, movewindow, l
bind = ALT SHIFT, J, movewindow, d
bind = ALT SHIFT, K, movewindow, u
bind = ALT SHIFT, L, movewindow, r

# === Column Navigation ===
bind = ALT, Home, focuswindow, first
bind = ALT, End, focuswindow, last

# === Monitor Navigation ===
bind = ALT CTRL, left, focusmonitor, l
bind = ALT CTRL, right, focusmonitor, r
bind = ALT CTRL, H, focusmonitor, l
bind = ALT CTRL, J, focusmonitor, d
bind = ALT CTRL, K, focusmonitor, u
bind = ALT CTRL, L, focusmonitor, r

# === Move to Monitor ===
bind = ALT SHIFT CTRL, left, movewindow, mon:l
bind = ALT SHIFT CTRL, down, movewindow, mon:d
bind = ALT SHIFT CTRL, up, movewindow, mon:u
bind = ALT SHIFT CTRL, right, movewindow, mon:r
bind = ALT SHIFT CTRL, H, movewindow, mon:l
bind = ALT SHIFT CTRL, J, movewindow, mon:d
bind = ALT SHIFT CTRL, K, movewindow, mon:u
bind = ALT SHIFT CTRL, L, movewindow, mon:r

# === Workspace Navigation ===
bind = ALT, Page_Down, workspace, e+1
bind = ALT, Page_Up, workspace, e-1
bind = ALT, bracketright, workspace, e+1
bind = ALT, bracketleft, workspace, e-1
bind = ALT, U, workspace, e+1
bind = ALT, I, workspace, e-1
bind = ALT CTRL, down, movetoworkspace, e+1
bind = ALT CTRL, up, movetoworkspace, e-1
bind = ALT CTRL, U, movetoworkspace, e+1
bind = ALT CTRL, I, movetoworkspace, e-1

# === Move Workspaces ===
bind = ALT SHIFT, Page_Down, movetoworkspace, e+1
bind = ALT SHIFT, Page_Up, movetoworkspace, e-1
bind = ALT SHIFT, U, movetoworkspace, e+1
bind = ALT SHIFT, I, movetoworkspace, e-1

# === Mouse Wheel Navigation ===
bind = ALT, mouse_down, workspace, e+1
bind = ALT, mouse_up, workspace, e-1
bind = ALT CTRL, mouse_down, movetoworkspace, e+1
bind = ALT CTRL, mouse_up, movetoworkspace, e-1

# === Numbered Workspaces ===
# bind = ALT, 1, workspace, 1
# bind = ALT, 2, workspace, 2
# bind = ALT, 3, workspace, 3
# bind = ALT, 4, workspace, 4
# bind = ALT, 5, workspace, 5
# bind = ALT, 6, workspace, 6
# bind = ALT, 7, workspace, 7
# bind = ALT, 8, workspace, 8
# bind = ALT, 9, workspace, 9

# === Move to Numbered Workspaces ===
bind = ALT SHIFT, 1, movetoworkspace, 1
bind = ALT SHIFT, 2, movetoworkspace, 2
bind = ALT SHIFT, 3, movetoworkspace, 3
bind = ALT SHIFT, 4, movetoworkspace, 4
bind = ALT SHIFT, 5, movetoworkspace, 5
bind = ALT SHIFT, 6, movetoworkspace, 6
bind = ALT SHIFT, 7, movetoworkspace, 7
bind = ALT SHIFT, 8, movetoworkspace, 8
bind = ALT SHIFT, 9, movetoworkspace, 9

# === Column Management ===
# bind = ALT, bracketleft, layoutmsg, preselect l
# bind = ALT, bracketright, layoutmsg, preselect r

# === Sizing & Layout ===
bind = ALT, R, layoutmsg, togglesplit
bind = ALT CTRL, F, resizeactive, exact 100%

# === Move/resize windows with mainMod + LMB/RMB and dragging ===
bindmd = ALT, mouse:272, Move window, movewindow
bindmd = ALT, mouse:273, Resize window, resizewindow

# === Move/resize windows with mainMod + LMB/RMB and dragging ===
bindd = ALT, code:20, Expand window left, resizeactive, -100 0
bindd = ALT, code:21, Shrink window left, resizeactive, 100 0

# === Manual Sizing ===
binde = ALT, minus, resizeactive, -10% 0
binde = ALT, equal, resizeactive, 10% 0
binde = ALT SHIFT, minus, resizeactive, 0 -10%
binde = ALT SHIFT, equal, resizeactive, 0 10%

# === Screenshots ===
bind = , Print, exec, dms screenshot
bind = CTRL, Print, exec, dms screenshot full
bind = ALT, Print, exec, dms screenshot window

# === System Controls ===
bind = ALT SHIFT, P, dpms, toggle
