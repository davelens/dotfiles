#!/usr/bin/env bash

set -e

if grep -qEi "(microsoft|WSL)" /proc/version; then
  echo 'windows'
else
  local OS=`uname -s`
  [ $OS == 'Darwin' ] && echo 'macos'
  [ $OS == 'Linux' ] && echo 'linux'
  [ $OS == 'FreeBSD' ] && echo 'freebsd'
fi
