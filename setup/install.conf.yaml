- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

# Requires 4 space indentation or it will fail
- link:
    ~/.ackrc: config/ackrc
    ~/.bash_profile: config/bash_profile
    ~/.bashrc: config/bashrc
    ~/.config/starship.toml: config/starship.toml
    ~/.editrc: config/editrc
    ~/.gitconfig: config/gitconfig
    ~/.gitignore_global: config/gitignore_global
    ~/.inputrc: config/inputrc
    ~/.rspec: config/rspec
    ~/.rsyncignore: config/rsyncignore
    ~/.rubocop.yml: config/rubocop.yml
    ~/.shellcheckrc: config/shellcheckrc
    ~/.local/bin/os: bin/os
    ~/.local/bin/utility: bin/utility
    $XDG_CONFIG_HOME/nethackrc: config/nethackrc
    $XDG_CONFIG_HOME/npm/npmrc: config/npmrc
    $XDG_CONFIG_HOME/wgetrc: config/wgetrc
    $XDG_CONFIG_HOME/shellcheckrc: config/shellcheckrc
    $XDG_CONFIG_HOME/wezterm/wezterm.lua: config/wezterm.lua

- shell:
  - [git submodule update --init --recursive, Installing submodules]
